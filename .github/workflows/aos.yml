name : Shell Commands

on: [push]

jobs:
    run-shell-command:
      runs-on: macos-latest
      steps:
        - name: echo a string
          run: echo "Hello World"
        - name: ltiline script
          run:  |
              node -v
              node -v
